#!/bin/bash

branch=$1

if [ -z "$branch" ]; then
  echo "Falta nombre de branch"
  exit 1
fi

git switch -c "$branch" || exit 1

if git remote | grep -q origin; then
  git push -u origin "$branch"
fi
